import styled from 'styled-components';

export const WrapperLayout = styled.div`
  position: relative;
  min-height: 100vh;
  .content {
    position: relative;
    min-height: 89.5vh;
    flex: 1;
    margin: 0 25%;
  }
`;
